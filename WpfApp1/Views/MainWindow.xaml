<Window x:Class="WallE.PixelArt.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WallE.PixelArt.Views"
        xmlns:vm="clr-namespace:WallE.PixelArt.ViewModels"
        xmlns:utils="clr-namespace:WallE.PixelArt.Utils"
        mc:Ignorable="d"
        Title="Wall-E Pixel Art" Height="600" Width="800">

	<Window.Resources>
		<utils:ColorConverter x:Key="ColorConverter"/>
	</Window.Resources>

	<Window.DataContext>
		<vm:MainViewModel/>
	</Window.DataContext>

	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<!-- Editor de Código -->
		<GroupBox Grid.Column="0" Header="Editor de Código" Margin="5">
			<TextBox Text="{Binding CodeText, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True" FontFamily="Consolas" FontSize="12"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
		</GroupBox>

		<!-- Panel de Control -->
		<StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center" Margin="10">
			<Label Content="Tamaño Canvas:"/>
			<TextBox Text="{Binding CanvasSize}" Width="50" Margin="0,5"/>
			<Button Content="Redimensionar" Command="{Binding ResizeCommand}" Margin="0,10" Padding="10,5"/>
			<Button Content="Ejecutar Código" Command="{Binding ExecuteCommand}" Margin="0,10" Padding="10,5"/>
			<Button Content="Guardar" Margin="0,5" Padding="10,5"/>
			<Button Content="Cargar" Margin="0,5" Padding="10,5"/>
		</StackPanel>

		<!-- Canvas de Píxeles -->
		<GroupBox Grid.Column="2" Header="Canvas" Margin="5">
			<ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
				<ItemsControl ItemsSource="{Binding Canvas.Pixels}" Background="LightGray">
					<ItemsControl.ItemsPanel>
						<ItemsPanelTemplate>
							<UniformGrid Columns="{Binding Canvas.Size}" Rows="{Binding Canvas.Size}"/>
						</ItemsPanelTemplate>
					</ItemsControl.ItemsPanel>

					<ItemsControl.ItemTemplate>
						<DataTemplate>
							<Border BorderBrush="LightGray" BorderThickness="0.5">
								<Rectangle Fill="{Binding Color, Converter={StaticResource ColorConverter}}"/>
							</Border>
						</DataTemplate>
					</ItemsControl.ItemTemplate>
				</ItemsControl>
			</ScrollViewer>
		</GroupBox>
	</Grid>
</Window>